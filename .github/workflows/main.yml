# ワークフロー名を定義
name: Hello GitHub Actions
# トリガーとなるイベントを定義(mainブランチへのpush)
on:
  push:
    branches:
      - 'main'
# ジョブの追加(最新バージョンをUbuntu上でbuildを実行)
jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Run a one-line script
        run: echo Hello GitHub Actions!!